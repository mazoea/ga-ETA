on:
  push:
  workflow_dispatch:

name: Test
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - uses: ./
      with:
        CONTEXT_GITHUB: ${{ toJson(github) }}